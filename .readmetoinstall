echo ".cfg" >> .gitignore
git clone <remote-git-repo-url> $HOME/.cfg
alias dotfiles='/usr/bin/git --git-dir=<path to .cfg’s Git directory> --work-tree=$HOME'
dotfiles config --local status.showUntrackedFiles no
dotfiles checkout

TO DO:
Automate Starship, zsh, neovim, update-alternatives, oh-my-zsh (It's all I can think of right now) to auto-install. Then do above.

Explore foot config

Explore Hyprland config

Explore Wofi config
